# OpenDeck Project Prerequisites & System Toolchain Checklist
# -------------------------------------------------------------
# OpenDeck is built using Tauri (Rust) for the desktop companion app (macOS/Windows)
# and Flutter (Dart) for the mobile app (iOS/Android).

# 1. Desktop Companion (Tauri & Rust Toolchain)
rustc>=1.75.0
cargo>=1.75.0
cargo-tauri>=1.5.0

# 2. Mobile Application (Flutter SDK)
flutter>=3.19.0
dart>=3.3.0

# 3. Desktop UI Scaffolding & Package Manager
node>=18.0.0
npm>=9.0.0

# 4. Optional Helper Tools & Utilities
fastlane>=2.210.0
